apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-ts-app.fullname" . }}
  labels:
    {{- include "express-ts-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-ts-app.selectorLabels" . | nindent 4 }}
