apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "express-ts-app.fullname" . }}-test-connection"
  labels:
    {{- include "express-ts-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "express-ts-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
